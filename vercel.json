{
  "version": 2,
  "builds": [
    {
      "src": "package.json",
      "use": "@frontity/now",
      "config": {
        "installCommand": "npm install --legacy-peer-deps",
        "buildCommand": "frontity build"
      }
    }
  ]
}